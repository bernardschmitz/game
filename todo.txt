
delete singletons!

make a path class

have weapon and bomb class

maybe lerp the background palette...

have a rendertarget that maintains a list of polygon.  
draws transparent triangles correctly.



lua error handler is _ALERT.  redefine like this:

static int LuaError( lua_State *L ) {

  const char *errormsg = lua_tostring( L, -1 );
  // ...do whatever U wanna do with errormsg...
  return 0;
}

// (..)
    lua_State *state = lua_open();
    lua_register( state, "_ALERT", LuaError );
// (..)


=====

polygon counter...

export from blender in a format for glDrawElements.


$Id: todo.txt,v 1.12 2003-08-06 02:40:22 bernard Exp $
